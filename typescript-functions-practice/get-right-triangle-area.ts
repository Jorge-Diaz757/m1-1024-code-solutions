/* exported getRightTriangleArea */

function getRightTriangleArea(base: number, height: number): number {
  const multiply: number = base * height;
  const area: number = multiply * 0.5;
  return area;
}
