/* exported getSumOfSquares */
function getSumOfSquares(x: number, y: number): number {
  const multiplyX: number = x * x;
  const multiplyY: number = y * y;
  const sum: number = multiplyX + multiplyY;
  return sum;
}
